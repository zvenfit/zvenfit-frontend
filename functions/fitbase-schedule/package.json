{
  "name": "zvenfit-fitbase-schedule",
  "version": "1.0.0",
  "private": true,
  "description": "Public schedule proxy for zvenfit.ru (Fitbase API v2)",
  "scripts": {
    "build": "rm -rf build && tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "npm run typecheck && npm run test:unit && npm run test:artifact",
    "test:unit": "node --import tsx --test src/__tests__/*.test.ts src/fitbase/__tests__/*.test.ts src/observability/__tests__/*.test.ts",
    "test:artifact": "npm run build && node --test src/__tests__/deployment.test.cjs"
  },
  "dependencies": {
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}
